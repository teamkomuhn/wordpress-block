// This wp Objects is a global Object added by WordPress automatically to interact with it.

// namespace/name so there is no conflicts
wp.blocks.registerBlockType('namespace/name', {
    title: x,
    icon: x,
    category: x,
    attributes: x,
    edit: x,
    save: x
})
